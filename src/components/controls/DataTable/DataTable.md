Basic component:

```js
import * as MUI from '@material-ui/core';
import {FormGenerator} from "../../FormGenerator";
import {basicTable} from '../../../data/dataTable';

<FormGenerator library={MUI} data={basicTable} guid="dataTable"/>
```
